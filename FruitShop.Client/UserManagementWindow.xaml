<Window x:Class="FruitShop.Client.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản Lý Người Dùng - FruitShop Admin" Height="650" Width="1050"
        WindowStartupLocation="CenterOwner" Background="#F5F7F4">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Quản lý Người Dùng &amp; Phân Quyền" FontSize="24" FontWeight="Bold" Foreground="#1D2A20"/>
                <TextBlock Text="Quản lý danh sách tài khoản Admin, Manager, Staff và Customer" FontSize="13" Foreground="#59635B" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Tải lại" Click="RefreshButton_Click" Width="100" Height="36" Background="#E2E8F0" Foreground="#1D2A20" BorderBrush="Transparent" FontWeight="SemiBold" Margin="0,0,10,0"/>
                <Button Content="+ Tạo Tài Khoản" Click="AddUserButton_Click" Width="140" Height="36" Background="#1F4D30" Foreground="White" BorderBrush="Transparent" FontWeight="SemiBold"/>
            </StackPanel>
        </DockPanel>

        <!-- Search / Filter -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="14" Margin="0,0,0,16" BorderBrush="#E2E8F0" BorderThickness="1">
            <DockPanel>
                <TextBlock Text="Danh sách người dùng hệ thống" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="15" Foreground="#1D2A20"/>
                <TextBlock x:Name="StatusTextBlock" Text="" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="#26733A" FontWeight="SemiBold"/>
            </DockPanel>
        </Border>

        <!-- Users DataGrid -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="1">
            <DataGrid x:Name="UsersDataGrid" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" RowHeight="40" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Tên đăng nhập" Binding="{Binding Username}" Width="130"/>
                    <DataGridTextColumn Header="Họ và tên" Binding="{Binding FullName}" Width="170"/>
                    <DataGridTextColumn Header="Vai trò (Role)" Binding="{Binding RoleName}" Width="110"/>
                    <DataGridTextColumn Header="Chi nhánh" Binding="{Binding BranchName}" Width="180"/>
                    <DataGridTextColumn Header="Email / SĐT" Binding="{Binding Email}" Width="160"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding IsActive}" Width="100"/>
                    <DataGridTemplateColumn Header="Thao tác" Width="180" MinWidth="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Khóa/Mở" Click="ToggleActive_Click" Margin="2,0" Padding="8,4" Background="#EDF2F7" Foreground="#2D3748" BorderBrush="Transparent" FontSize="12"/>
                                    <Button Content="Reset MK" Click="ResetPassword_Click" Margin="2,0" Padding="8,4" Background="#FEFCBF" Foreground="#744210" BorderBrush="Transparent" FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer -->
        <DockPanel Grid.Row="3" Margin="0,16,0,0">
            <Button Content="Đóng" Click="CloseButton_Click" Width="100" Height="36" DockPanel.Dock="Right" Background="#CBD5E0" Foreground="#2D3748" BorderBrush="Transparent" FontWeight="SemiBold"/>
        </DockPanel>
    </Grid>
</Window>